# 架構規劃

## 專案目標
個人作品集網站，展示前端工程師的技能、學經歷、作品，以及持續學習的軌跡（刷題紀錄、技術筆記）。

## 技術棧
| 分類 | 選擇 | 說明 |
|---|---|---|
| 框架 | Nuxt 4 | SSR/SSG，SEO 與首屏效能優於 Angular Universal，且能與現有 Angular 作品互補展示技術廣度 |
| 語言 | TypeScript | 全站型別安全 |
| 樣式 | Tailwind CSS v4 | 透過 `@tailwindcss/vite` 整合，開發效率高 |
| UI 元件庫 | shadcn-vue | 基於 reka-ui，元件原始碼直接進專案（`app/components/ui`），可完全客製化樣式 |
| 內容管理 | Nuxt Content | 以 Markdown 管理筆記集合、作品說明，免資料庫、可版本控管 |
| 部署 | Vercel / Cloudflare Pages | 兩者皆對 Nuxt SSR/SSG 支援良好，免費額度足夠個人網站 |
| 刷題資料 | 初期：手動維護 JSON<br>後期：串接第三方 API 或自建後端 | LeetCode 無官方公開 API，先求穩定上線，之後可用 ASP.NET Core 練 C# 順便做自動化（獨立 repo，不採 monorepo） |

### 已完成的初始化
- `nuxi init` 建立 Nuxt 4 minimal 專案（`app/` 目錄結構）
- Tailwind CSS v4 透過 `nuxt.config.ts` 的 `vite.plugins` 整合，樣式檔在 `app/assets/css/main.css`
- shadcn-vue 已 `init`，`components.json` 設定 alias 為 `@/components`、`@/lib` 等
- `nuxt.config.ts` 已加上 `components` 設定，讓 `app/components/ui/*` 底下的元件以原始檔名註冊（不加 `Ui` 前綴），避免與 shadcn 產生的 `index.ts` re-export 撞名

## 頁面架構（路由）
```
/                   首頁（導覽 + 摘要）
/skills             技能展示
/experience         學經歷展示
/works              作品展示（含 ui.sanring.dev、haul.sanring.dev 連結）
/works/[slug]       單一作品詳情
/leetcode           刷題 dashboard（解題數、難度分布、標籤分布）
/notes              筆記集合（列表）
/notes/[slug]       單篇筆記內容
```

## 資料管理策略
- **技能 / 學經歷**：`content/skills.json`、`content/experience.json`，結構化資料，方便未來改成表格或時間軸呈現。
- **作品展示**：`content/works/*.md`，每篇 frontmatter 含 title、tech stack、demo/repo 連結、封面圖。
- **刷題 dashboard**：`content/leetcode.json` 手動維護，欄位包含 date、title、difficulty、tags、solution 連結。之後若做自動化，改由後端 API（ASP.NET Core）定期抓取並提供 JSON。
- **筆記集合**：`content/notes/*.md`，用 Nuxt Content 的 frontmatter 管理分類、標籤、發布日期。

## 目錄結構（預計）
```
portfolio/
├─ app.vue
├─ nuxt.config.ts
├─ pages/
│  ├─ index.vue
│  ├─ skills.vue
│  ├─ experience.vue
│  ├─ works/
│  │  ├─ index.vue
│  │  └─ [slug].vue
│  ├─ leetcode.vue
│  └─ notes/
│     ├─ index.vue
│     └─ [slug].vue
├─ components/
├─ content/
│  ├─ skills.json
│  ├─ experience.json
│  ├─ leetcode.json
│  ├─ works/
│  └─ notes/
├─ public/
└─ 架構.md
```

## 開發優先順序
1. 首頁 / 技能展示 / 學經歷 / 作品展示（核心頁面，靜態資料即可）
2. 筆記集合（Nuxt Content，邊學 C#/NestJS 邊發布）
3. 刷題 dashboard（先手動 JSON，穩定後再評估自動化）

## 未來規劃
- 用 ASP.NET Core（C#）開一支小型 API，負責刷題資料或筆記的 CRUD，前後端分離，同時作為 C# 學習的實作練習，並可在作品展示中新增一筆全端作品。
